version: "3"

services:
  ingredient:
    build: ./server
    container_name: doughBros_server
    command: npm start
    depends_on:
      - "db"
    ports:
      - "8080:8080"
    volumes:
      - ./server/:/usr/app/src
      - ./config/:/usr/app/config

  db:
    container_name: doughBros_db
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: doughBros_db
      MYSQL_USER: user
      MYSQL_PASSWORD: root
    ports:
      - "3307:3306"
    volumes:
      - ./database/data:/var/lib/mysql
      - ./database/init.sql:/dockerInit/init.sql

